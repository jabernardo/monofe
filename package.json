{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff",
    "dev:run-app": "yarn workspace @monofe/app run dev",
    "dev:run-client": "yarn workspace @monofe/components run dev",
    "dev": "npm-run-all --parallel dev:*"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "name": "monofe"
}
